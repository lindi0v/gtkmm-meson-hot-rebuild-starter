{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug some-app (LLDB)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/some-app",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "externalConsole": true,
      "MIMode": "lldb",
      "preLaunchTask": "Meson: build"
    }
  ]
}
